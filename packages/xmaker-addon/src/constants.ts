/**
 * Constantes do XMaker Addon
 */
export const ADDON_ID = 'fremux/xmaker';
export const PANEL_ID = `${ADDON_ID}/panel`;
export const PARAM_KEY = 'xmaker';

// Eventos específicos do XMaker
export const EVENTS = {
  GENERATE_COMPONENT: `${ADDON_ID}/generate`,
  UPDATE_CONFIG: `${ADDON_ID}/update-config`,
  PREVIEW_COMPONENT: `${ADDON_ID}/preview`,
  EXPORT_CODE: `${ADDON_ID}/export-code`,
};
